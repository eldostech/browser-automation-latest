## The task

$task

$brief

## Where

Start at: $start_url

## What you have been given

Credential slots you may bind with `mark_as_secret`: $secrets

A sample record to work with: $sample

If a sample record is given, do the task for **that** record, and mark the
values that came from it as inputs. If none is given, pick one from the site
and say which you chose.

Begin by taking a snapshot.
